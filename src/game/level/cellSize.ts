/**
 * Macro-cell dimensions, split out from `expand.ts` so the relief height field
 * can depend on them without importing the expander (which imports relief).
 */

export const CELL_W = 20;
export const CELL_H = 12;
